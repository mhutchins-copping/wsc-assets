node_modules/
dist/
.vite/
worker/node_modules/
.claude/
.env
.env.*
*.pem
credentials.json
# Local safety exports written by scripts/restore-db.sh. Backups live in
# GitHub Actions artifacts, not in the repo.
backups/
# Wrangler local state (account ID cache, D1 preview shards, tmp dirs).
# Regenerated on `wrangler deploy` / `wrangler dev`.
.wrangler/
worker/.wrangler/
intune-audit/
